diff --git a/hermes_cli/web_models.py b/hermes_cli/web_models.py
index fa5dd37243..f2531df373 100644
--- a/hermes_cli/web_models.py
+++ b/hermes_cli/web_models.py
@@ -60,6 +60,9 @@ class CustomEndpointUpdate(BaseModel):
     discover_models: bool = True
     make_default: bool = False
     models: Optional[List[str]] = None
+    # True = replace the stored model map with body.models entirely (filtered
+    # catalogue), instead of the default merge that keeps existing entries.
+    replace_models: bool = False
 
 
 class MessagingPlatformUpdate(BaseModel):
diff --git a/hermes_cli/web_server.py b/hermes_cli/web_server.py
index 2162c15473..c056d87325 100644
--- a/hermes_cli/web_server.py
+++ b/hermes_cli/web_server.py
@@ -8666,14 +8666,21 @@ def _write_custom_endpoint(cfg: Dict[str, Any], body: CustomEndpointUpdate) -> T
         "model": model,
         "discover_models": bool(body.discover_models),
     })
-    # Same for the model map: merge rather than replace, so existing models
-    # keep their context lengths. ``body.models`` is the catalogue the panel's
-    # Test button already discovered — without it only the one hand-typed
+    # Same for the model map: merge rather than replace by default, so existing
+    # models keep their context lengths. ``body.models`` is the catalogue the
+    # panel's Test button already discovered — without it only the one hand-typed
     # model survived Save, and every picker showed a single-entry list for a
     # provider serving dozens (#69988). A payload with no ``models`` (older
     # UI) still just ensures the named default is present.
     existing_models = entry.get("models")
     models_map: Dict[str, Any] = dict(existing_models) if isinstance(existing_models, dict) else {}
+    if body.replace_models and body.models:
+        # Filtered catalogue: replace the stored list entirely instead of
+        # merging, so GUI-side model filtering actually shrinks the config.
+        # A filtered list is a pinned catalogue — auto-discovery would just
+        # re-pull the full list on the next refresh, undoing the filter.
+        models_map = {}
+        entry["discover_models"] = False
     for candidate in (*(body.models or ()), model):
         model_id = str(candidate).strip()
         if not model_id:
diff --git a/web/src/components/EndpointFormDialog.tsx b/web/src/components/EndpointFormDialog.tsx
index 1982ad96c0..bdddd0d5ed 100644
--- a/web/src/components/EndpointFormDialog.tsx
+++ b/web/src/components/EndpointFormDialog.tsx
@@ -1,4 +1,4 @@
-import { useCallback, useState } from "react";
+import { useCallback, useEffect, useMemo, useState } from "react";
 import { CheckCircle2, XCircle } from "lucide-react";
 import { Button } from "@nous-research/ui/ui/components/button";
 import { Input } from "@nous-research/ui/ui/components/input";
@@ -60,11 +60,28 @@ export function EndpointFormDialog({ endpoint, onClose, onSaved }: Props) {
   const [makeDefault, setMakeDefault] = useState(false);
 
   const [fetchedModels, setFetchedModels] = useState<string[]>(endpoint?.models ?? []);
+  const [filterText, setFilterText] = useState("");
   const [validMsg, setValidMsg] = useState("");
   const [validOk, setValidOk] = useState(false);
   const [testing, setTesting] = useState(false);
   const [saving, setSaving] = useState(false);
 
+  // Live-filter the fetched catalogue (e.g. type "free" to keep only free
+  // models). Empty filter = show everything.
+  const filteredModels = useMemo(() => {
+    const q = filterText.trim().toLowerCase();
+    if (!q) return fetchedModels;
+    return fetchedModels.filter((m) => m.toLowerCase().includes(q));
+  }, [fetchedModels, filterText]);
+
+  // Keep the default model inside the filtered set; if the filter removes the
+  // current default, fall back to the first remaining match.
+  useEffect(() => {
+    if (filteredModels.length > 0 && !filteredModels.includes(model)) {
+      setModel(filteredModels[0]);
+    }
+  }, [filteredModels, model]);
+
   const doTest = useCallback(async () => {
     if (!baseUrl.trim()) {
       showToast("Enter an endpoint URL first.", "error");
@@ -109,7 +126,8 @@ export function EndpointFormDialog({ endpoint, onClose, onSaved }: Props) {
         api_key: apiKey || null,
         discover_models: discover,
         make_default: makeDefault,
-        ...(fetchedModels.length ? { models: fetchedModels } : {}),
+        ...(filteredModels.length ? { models: filteredModels } : {}),
+        ...(filterText.trim() ? { replace_models: true } : {}),
       };
       const res = await apiFetch<{ ok: boolean }>("/api/providers/custom-endpoints", { method: "POST", body: JSON.stringify(body) });
       if (res.ok) {
@@ -123,7 +141,7 @@ export function EndpointFormDialog({ endpoint, onClose, onSaved }: Props) {
     } finally {
       setSaving(false);
     }
-  }, [name, baseUrl, model, apiKey, discover, makeDefault, fetchedModels, endpoint, showToast, t.endpoints.saveOk, t.endpoints.saveFail, onSaved]);
+  }, [name, baseUrl, model, apiKey, discover, makeDefault, fetchedModels, filterText, filteredModels, endpoint, showToast, t.endpoints.saveOk, t.endpoints.saveFail, onSaved]);
 
   return (
     <div className="fixed inset-0 z-50 flex items-end sm:items-center justify-center bg-black/50">
@@ -167,15 +185,30 @@ export function EndpointFormDialog({ endpoint, onClose, onSaved }: Props) {
           <div className="space-y-1.5">
             <Label className="text-xs sm:text-sm">{t.endpoints.formModel}</Label>
             {fetchedModels.length > 0 ? (
-              <select
-                value={model}
-                onChange={(e) => setModel(e.target.value)}
-                className="w-full min-h-[44px] rounded-md border bg-background px-3 py-2.5 text-sm"
-              >
-                {fetchedModels.map((m) => (
-                  <option key={m} value={m}>{m}</option>
-                ))}
-              </select>
+              <>
+                <Input
+                  value={filterText}
+                  onChange={(e) => setFilterText(e.target.value)}
+                  placeholder={t.endpoints.formFilterHint}
+                  className="mb-1.5"
+                />
+                {filteredModels.length > 0 ? (
+                  <select
+                    value={model}
+                    onChange={(e) => setModel(e.target.value)}
+                    className="w-full min-h-[44px] rounded-md border bg-background px-3 py-2.5 text-sm"
+                  >
+                    {filteredModels.map((m) => (
+                      <option key={m} value={m}>{m}</option>
+                    ))}
+                  </select>
+                ) : (
+                  <p className="text-xs text-red-500">{t.endpoints.formFilterEmpty}</p>
+                )}
+                <p className="text-xs text-muted-foreground">
+                  {filterText.trim() ? `${filteredModels.length} / ${fetchedModels.length}` : `${fetchedModels.length} models`}
+                </p>
+              </>
             ) : (
               <Input value={model} onChange={(e) => setModel(e.target.value)} placeholder="e.g. qwen3.8-max" />
             )}
diff --git a/web/src/i18n/af.ts b/web/src/i18n/af.ts
index 9b472cf0bc..c0c2137af2 100644
--- a/web/src/i18n/af.ts
+++ b/web/src/i18n/af.ts
@@ -805,5 +805,7 @@ export const af: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/ar.ts b/web/src/i18n/ar.ts
index e40f874799..be1fd1f75a 100644
--- a/web/src/i18n/ar.ts
+++ b/web/src/i18n/ar.ts
@@ -741,5 +741,7 @@ export const ar = defineLocale({
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 });
diff --git a/web/src/i18n/de.ts b/web/src/i18n/de.ts
index 72a88c740c..be359739d0 100644
--- a/web/src/i18n/de.ts
+++ b/web/src/i18n/de.ts
@@ -804,5 +804,7 @@ export const de: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/en.ts b/web/src/i18n/en.ts
index fbadae4d1d..23057f6618 100644
--- a/web/src/i18n/en.ts
+++ b/web/src/i18n/en.ts
@@ -906,5 +906,7 @@ export const en: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/es.ts b/web/src/i18n/es.ts
index 88b347bdf5..f84ce2d8cf 100644
--- a/web/src/i18n/es.ts
+++ b/web/src/i18n/es.ts
@@ -805,5 +805,7 @@ export const es: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/fr.ts b/web/src/i18n/fr.ts
index 2b26e0871f..efbfd1c6fe 100644
--- a/web/src/i18n/fr.ts
+++ b/web/src/i18n/fr.ts
@@ -805,5 +805,7 @@ export const fr: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/ga.ts b/web/src/i18n/ga.ts
index 3da0c109c7..1718626f9b 100644
--- a/web/src/i18n/ga.ts
+++ b/web/src/i18n/ga.ts
@@ -813,5 +813,7 @@ export const ga: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/hu.ts b/web/src/i18n/hu.ts
index 8ede649c07..3e8acc5dd5 100644
--- a/web/src/i18n/hu.ts
+++ b/web/src/i18n/hu.ts
@@ -805,5 +805,7 @@ export const hu: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/it.ts b/web/src/i18n/it.ts
index 641c7764d7..19768ab857 100644
--- a/web/src/i18n/it.ts
+++ b/web/src/i18n/it.ts
@@ -804,5 +804,7 @@ export const it: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/ja.ts b/web/src/i18n/ja.ts
index 7a7cbc99de..818e78be74 100644
--- a/web/src/i18n/ja.ts
+++ b/web/src/i18n/ja.ts
@@ -804,5 +804,7 @@ export const ja: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/ko.ts b/web/src/i18n/ko.ts
index ab5dfd3601..4f5f516a02 100644
--- a/web/src/i18n/ko.ts
+++ b/web/src/i18n/ko.ts
@@ -804,5 +804,7 @@ export const ko: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/pt.ts b/web/src/i18n/pt.ts
index 883bf54b10..16c5340866 100644
--- a/web/src/i18n/pt.ts
+++ b/web/src/i18n/pt.ts
@@ -806,5 +806,7 @@ export const pt: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/ru.ts b/web/src/i18n/ru.ts
index d53660f404..a3885576c4 100644
--- a/web/src/i18n/ru.ts
+++ b/web/src/i18n/ru.ts
@@ -805,5 +805,7 @@ export const ru: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/tr.ts b/web/src/i18n/tr.ts
index 96bbda2b76..72748a20ee 100644
--- a/web/src/i18n/tr.ts
+++ b/web/src/i18n/tr.ts
@@ -805,5 +805,7 @@ export const tr: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/types.ts b/web/src/i18n/types.ts
index ce13c5a4a4..d49fda8946 100644
--- a/web/src/i18n/types.ts
+++ b/web/src/i18n/types.ts
@@ -901,5 +901,7 @@ export interface Translations {
     formApiKeyHint: string;
     formDiscover: string;
     formMakeDefault: string;
+    formFilterHint: string;
+    formFilterEmpty: string;
   };
 }
diff --git a/web/src/i18n/uk.ts b/web/src/i18n/uk.ts
index 464f16fb10..6486bbc88c 100644
--- a/web/src/i18n/uk.ts
+++ b/web/src/i18n/uk.ts
@@ -806,5 +806,7 @@ export const uk: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "Filter models (e.g. free — saves only matches)",
+    formFilterEmpty: "No models match the filter",
   },
 };
diff --git a/web/src/i18n/zh-hant.ts b/web/src/i18n/zh-hant.ts
index 7ec2c944cf..6f6e04d715 100644
--- a/web/src/i18n/zh-hant.ts
+++ b/web/src/i18n/zh-hant.ts
@@ -804,5 +804,7 @@ export const zhHant: Translations = {
     formApiKeyHint: "Stored in ~/.hermes/.env, never in config.yaml",
     formDiscover: "Discover models automatically",
     formMakeDefault: "Activate as default after saving",
+    formFilterHint: "筛选模型（如填 free，保存时仅保留匹配项）",
+    formFilterEmpty: "没有匹配的模型",
   },
 };
diff --git a/web/src/i18n/zh.ts b/web/src/i18n/zh.ts
index 1d787bca23..91f1c3de01 100644
--- a/web/src/i18n/zh.ts
+++ b/web/src/i18n/zh.ts
@@ -800,5 +800,7 @@ export const zh: Translations = {
     formApiKeyHint: "存储在 ~/.hermes/.env，绝不进 config.yaml",
     formDiscover: "自动发现模型",
     formMakeDefault: "保存后设为默认激活",
+    formFilterHint: "筛选模型（如填 free，保存时仅保留匹配项）",
+    formFilterEmpty: "没有匹配的模型",
   },
 };
